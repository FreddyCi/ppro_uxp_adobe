import React from 'react';
import type { LumaVideoFormProps } from './types';

export const LumaVideoForm: React.FC<LumaVideoFormProps> = (props) => {
  // This will be populated in the next task with the video generation UI
  return (
    <div className="luma-video-form">
      <p className="text-detail">Video form will be implemented in the next step...</p>
    </div>
  );
};
