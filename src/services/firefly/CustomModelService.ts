/**
 * Custom models were previously used for third-party Firefly endpoints that
 * require specialized enterprise entitlements. These calls triggered 403/404
 * responses for most environments and caused confusion in the panel.
 *
 * The service has been retired in favour of the standard Firefly generate
 * endpoints. This placeholder prevents accidental imports while keeping the
 * file for historical reference.
 */

export {}