import React from 'react';
import type { LumaImageFormProps } from './types';

export const LumaImageForm: React.FC<LumaImageFormProps> = (props) => {
  // This will be populated in the next task with the image generation UI
  return (
    <div className="luma-image-form">
      <p className="text-detail">Image form will be implemented in the next step...</p>
    </div>
  );
};
